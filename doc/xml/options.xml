<section id="options">
	<title>Options dialog</title>
	<para>
		The options dialog enables you to influence the behaviour and
		look of &wb-productname; to meet your needs. To open the options
		dialog choose
		<menuchoice><guimenu>Tools</guimenu><guimenuitem>Options</guimenuitem></menuchoice>.
	</para>

	<section id="options-general">
		<title>General options</title>

		<section id="options-automatic-update">
			<title>Check for updates</title>
			<para>
				With this option you can enable an automatic update check when
				&wb-productname; is started. You can define the interval in days
				after which the application should check for updates on the
				homepage. If a newer version is found on the website this will
				be indicated with a little globe in the statusbar. Clicking on the
				icon will open your default internet browser with the application's homepage.
			</para>
			<para>
				If you disable this option, you can manually check
				for updates using the menu
				<menuchoice><guimenu>Help</guimenu><guimenuitem>Check for updates...</guimenuitem></menuchoice>.
			</para>
		</section>

		<section id="options-gui-language">
			<title>Language</title>
			<para>
				With this option you can select in which language the application
        is shown. The new value will only be in affect when you restart the
        application.
			</para>
		</section>

		<section id="options-encrypt-password">
			<title>Encrypt passwords</title>
			<para>If this option is enabled, the password stored within a connection
				profile will be encrypted. Whether the password should be stored at all
				can be selected in the profile itself.
			</para>
			<note><para>
				Using this option only supplies very limited security. As the
				source code for &wb-productname; is freely available, the algorithm
				to decrypt the passwords stored in this way can easily be extracted
				to retrieve the plain text passwords.
			</para></note>
		</section>

		<section id="options-consolidate-log">
			<title>Consolidate script log</title>
			<para>
				Usually &wb-productname; reports the success and timings
				for each statement that is beeing executed in the message tab
				of the current SQL panel. For large scripts this can slow down
				script execution dramatically. If this option is enabled, only
				a summary of the execution is printed once the script has finished.
				You can turn off the log during script execution by using
				the <link linkend="command-wbfeedback">WBFEEDBACK</link> command.
			</para>
		</section>

		<section id="option-animated-icon" xreflabel="Enable animated icons">
			<title>Enable animated icons</title>
			<para>Enable or disable the use of an animated icons in the SQL editor to
				indicate a running SQL statement. It has been reported, that the animated
				icon does have a severe (negativ) impact on the performance on some computers
				(depending on JDK/OS/Graphics driver).
				If you experience a high CPU usage during the execution of SQL statements, or
				if you find your SQL statements are running very slow, try
				to turn off the usage of the animated icons.
			</para>
		</section>

		<section id="options-standard-font">
			<title>Standard font</title>
			<para>
				The standard font that is used for menus, lables, buttons etc.
			</para>
		</section>

		<section id="options-message-font">
			<title>Message font</title>
			<para>
				The font that is used in the message pane of the SQL window.
			</para>
		</section>

		<section id="options-field-delimiter">
			<title>Field delimiter</title>
			<para>The default delimiter used when exporting data to a text file.
				This can be overridden in the file selection dialog when actually
				exporting the data.
			</para>
		</section>

		<section id="options-quote-character">
			<title>Quote character</title>
			<para>The default character to be used to quote column data when exporting to
				a text file. This value will be used when you don't specify a quote
				character e.g. for the <link linkend="command-export"><literal>WbExport</literal></link>
				command and when you use <menuchoice><guimenu>SQL</guimenu><guimenuitem>Save data as</guimenuitem></menuchoice>
				to export a result set.
			</para>
		</section>

		<section id="options-pdf-reader">
			<title>PDF Reader</title>
			<para>
				If you want to display the PDF manual from inside the application, you need to
				specify the full path to your PDF reader application. If this is defined
				and the file <literal>SQLWorkbench-Manual.pdf</literal> is available in the
				directory where <literal>sqlworkbench.jar</literal> is located, you will be
				able to display the PDF manual by selecting
				<menuchoice><guimenu>Help</guimenu><guimenuitem>Manual</guimenuitem></menuchoice>.
			</para>
		</section>

		<section id="options-log-level">
			<title>Log Level</title>
			<para>
				With this option you can control the level of information written to the
				application log. The most verbose level is <literal>DEBUG</literal>. With
				<literal>ERROR</literal> only severe errors (either resulting from running
				a user command or from an internal error) are written to the application log.
			</para>
		</section>
	</section>

	<section id="options-editor">
		<title>Editor options</title>

		<section id="options-autojump-statement">
			<title>Auto jump next statement</title>
			<para>
				If this option is enabled, then the cursor will automatically jump
				to the next statement in the script, when you execute a single statement
				using <keycap>Ctrl-Enter</keycap> ("Run current statement"). This can also be
				toggled through the menu <menuchoice><guimenu>SQL</guimenu><guimenuitem>Jump to next statement</guimenuitem></menuchoice>
			</para>
			<para>For more information on how you can execute statements
				in the editor, please refer to <link linkend="execute-sql-commands">Executing Statements</link>
			</para>
		</section>

		<section id="settings-right-clickmove">
			<title>Right click behaviour in the editor</title>
			<para>
				Normally a right click in the SQL editor does not change the location
				of the cursor (caret). If this option is checked, then a right
				click will also change the caret's location (to where the mouse cursor
				is located)
			</para>
		</section>

		<section id="options-close-search">
			<title>Close completion with search</title>
			<para>
				When using the quicksearch feature in the <link linkend="command-completion">code completion</link>
				this option controls the behaviour when hitting the ESC key. If this option
				is enabled, the ESC key will also close the popup window with the available
				choices. If this option is disabled, the ESC key will only close the quicksearch
				<emphasis>input</emphasis> field.
			</para>
		</section>

		<section id="options-completion-paste-case">
			<title>Paste completion in</title>
			<para>
				With this option you can select how the selected object name
				from the code completion popup is pasted into the editor. <literal>As is</literal>
				means, that the values will be inserted into the editor as it was
				retrieved from the database. This option will also be used when SQL statements
				are generated internally (e.g. for updating the result set or when you
				export/copy data as SQL statements)
			</para>
		</section>

		<section id="options-sort-completion-by">
			<title>Sort pasted columns by</title>
			<para>
        When selecting to paste all (or several columns) from the popup window,
        you can select with this option, in which order the columns should be written
        into the editor.
			</para>
		</section>

		<section id="options-internal-lineending">
			<title>Line ending for DBMS</title>
			<para>
				This property controls the line terminator used by the editor when sending SQL statements
				to the database. The value "Platform default" relates to the platform where you
				run &wb-productname; this is not the platform of the DBMS server.
			</para>
			<para>
				The editor always uses "unix" line ending internally. If you select a different
				value for this property, &wb-productname; will convert the SQL statements to use
				the desired line ending before sending them to the DBMS. As this can slow down
				the execution of statements, it is highly recommended to leave the default setting
				of Unix line endings. You should only change this, if your DBMS does not understand
				the single linefeed character (ASCII value 10) properly.
			</para>
		</section>

		<section id="options-externally-lineending">
			<title>File format</title>
			<para>
				This property controls the line terminator used when a file is saved by the editor.
				Changing this property affects the next save operation.
			</para>
		</section>

		<section id="options-editor-font">
			<title>Editor font</title>
			<para>
				The font that is used in the SQL editor. This font is also
				used when displaying the SQL source for tables and other
				database objects in the <link linkend="dbexplorer">DbExplorer</link>.
			</para>
		</section>

		<section id="options-error-highlight">
			<title>Error highlight color</title>
			<para>
				When a statement is not executed correctly (and the DBMS signals
				an error) it is highlighted in the editor. With this option
				you can select the color that is used to highlight the incorrect
				statement.
			</para>
		</section>

		<section id="options-selection-color">
			<title>Selection color</title>
			<para>
				The color that is used to highlight selected text.
			</para>
		</section>

		<section id="options-alternate-delimiter" xreflabel="alternate delimiter">
			<title>Alternate Delimiter</title>
			<para>
				This options defines the default alternate delimiter. You can
				override this default in the connection profile, to use different
				delimiters for different DBMS.
				For details see <link linkend="understand-alternate-delim">using the alternate delimiter</link>
			</para>
		</section>

		<section id="options-history-size">
			<title>History size</title>
			<para>The number of statements per tab which should be stored in the
				statement history. Remember that always the full text of the editor
				(together with the selection and cursor information) is stored in the
				history. If you have large amounts of text in the editor and set this
				number quite high, be aware of the memory consumption this might create.
			</para>
		</section>

		<section id="options-files-in-history">
			<title>Files in history</title>
			<para>
        If this option is enabled, the content of external files is also
        stored in the statement history.
			</para>
		</section>

		<section id="settings-editor-electric-srcoll">
			<title>Electric scroll</title>
			<para>Electric scrolling is the automatic scrolling of the editor
				when clicking into lines close to the upper or lower end of the editor
				window. If you click inside the defined number of lines at the upper
				or lower end, then the editor will scroll this line into the center
				of the visible area. The default is set to 3, which means that if you click
				into (visible) line 1,2 or 3 of the editor, this line will be centered in
				the display.
			</para>
		</section>

		<section id="options-tab-width">
			<title>Editor tab width</title>
			<para>The number of spaces that are assumed for the <literal>TAB</literal>
				character.
			</para>
		</section>

		<section id="options-noword-sep">
			<title>Additional word characters</title>
			<para>
				The editor recognizes character sequences that consist of letters
				and characters only as "words". This influences the way word by word
				jumping is done, or when selecting text using a doubleclick.
				Every character that is entered for this option is considered a "word"
				character and thus does not mark a word boundary.
			</para>
			<para>
				By putting e.g. an underscore into this field, the text <literal>MY_TABLE</literal> is
				recognized as a single word instead of two words (which is the default).
			</para>
		</section>

	</section>

	<section id="options-data-display">
		<title>Options for displaying data</title>

		<section id="options-data-font">
			<title>Data font</title>
			<para>
				The font that is used to display result sets. This includes
				the object list and results in the <link linkend="dbexplorer">DbExplorer</link>.
			</para>
		</section>

		<section id="options-date-format">
			<title>Date, timestamp and time formats</title>
			<para>Define the format for displaying date, date/time (timestamp) and time columns in the result
				set. For details on the format of this option, please refer to the
				documentation of the <ulink url="http://java.sun.com/j2se/1.5.0/docs/api/java/text/SimpleDateFormat.html"><literal>SimpleDateFormat</literal></ulink> class.
				This format is also used when parsing input for date or timestamp fields, so if you
				enter a date while <link linkend="edit-data">editing</link> the data,
				make sure you enter it the same way as defined with this option.
			</para>
			<para>
				Here is an overview of the letters and their meaning that can be used to format
				the date and timestamp values. Be aware that case matters!
				<informaltable frame="all">
					<tgroup cols="2" align="left">
						<colspec colname="c1" colwidth="1cm" />
						<colspec colname="c2" />
						<thead>
							<row>
								<entry>Letter</entry>
								<entry>Description</entry>
							</row>
						</thead>
						<tbody valign="top">
							<row><entry>G</entry><entry>Era designator (Text, e.g. AD)</entry></row>
							<row><entry>y</entry><entry>Year (Number)</entry></row>
							<row><entry>M</entry><entry>Month in year (Number)</entry></row>
							<row><entry>w</entry><entry>Week in year (Number)</entry></row>
							<row><entry>W</entry><entry>Week in month (Number)</entry></row>
							<row><entry>D</entry><entry>Day in year (Number)</entry></row>
							<row><entry>d</entry><entry>Day in month (Number)</entry></row>
							<row><entry>F</entry><entry>Day of week in month (Number)</entry></row>
							<row><entry>E</entry><entry>Day in week (Text)</entry></row>
							<row><entry>a</entry><entry>AM/PM marker</entry></row>
							<row><entry>H</entry><entry>Hour in day (0-23)</entry></row>
							<row><entry>k</entry><entry>Hour in day (1-24)</entry></row>
							<row><entry>K</entry><entry>Hour in am/pm (0-11)</entry></row>
							<row><entry>h</entry><entry>Hour in am/pm (1-12)</entry></row>
							<row><entry>m</entry><entry>Minute in hour</entry></row>
							<row><entry>s</entry><entry>Second in minute</entry></row>
							<row><entry>S</entry><entry>Milliseconds</entry></row>
							<row><entry>z</entry><entry>General time zone (e.g. Pacific Standard Time; PST; GMT-08:00)</entry></row>
							<row><entry>Z</entry><entry>RFC 822 time zone (e.g. -0800)</entry></row>
						</tbody>
					</tgroup>
				</informaltable>
			</para>
		</section>

		<section id="options-ora-data-timestamp">
			<title>Oracle <literal>DATE</literal> as <literal>TIMESTAMP</literal></title>
			<para>
				The Oracle <literal>DATE</literal> datatype includes the time as well. But the 
				JDBC driver does not retrieve the time part of a DATE column, so when retrieving
				<literal>DATE</literal> values, this would remove the time stored in the database. If this option is
				enabled, &wb-productname; will treat Oracle's <literal>DATE</literal> columns
				as <literal>TIMESTAMP</literal> columns, thus preserving the time information.
			</para>
		</section>

		<section id="options-decimal-symbol">
			<title>Decimal symbol</title>
			<para>
				The character which is used as the decimal separator when displaying numbers.
			</para>
		</section>

		<section id="options-decimal-digits">
			<title>Decimal digits</title>
			<para>Define the maximum number of digits which will be displayed for
				numeric columns. This only affects the display of the number internally
				they are still stored as the DBMS returned them. To see the internal
				value, leave the mouse cursor over the cell. The tooltip which is
				displayed will contain the number as it was returned by the JDBC driver.
				When exporting data or copying it to the clipboard, the real value will
				be used.
			</para>
		</section>

		<section id="option-sort-collator">
      <title>Sort Locale</title>
      <para>
      	When you sort the result set, characters values will be sorted case-sensitiv by default.
      	This is caused by the <literal>compareTo()</literal> method available in the Java
      	environment which puts lower case characters in front of upper case characters when sorting.
				With the "Sort Locale" option you can select which language rules should be applied
				while sorting. Note that sorting with a locale is slower than using the "Default" setting.
      </para>
    </section>

		<section id="column-width-settings">
			<title>Column width settings</title>

			<simplesect id="options-auto-optimize-column-width">
				<title>Automatically adjust column widths</title>
				<para>
					If this option is enabled, the widths of the result set columns are automatically
					adjusted to fit the largest value (respecting the min. and max. size settings) after
					retrieving data. Note that you can manually optimize the column widths using
					<menuchoice><guimenu>View</guimenu><guimenuitem>Optimize width for all columns</guimenuitem></menuchoice>.
				</para>
			</simplesect>

			<simplesect id="options-optimize-include-header">
				<title>Adjust to column headers</title>
				<para>
					When calculating the optimal width for a column (either manually
					or if "Auto adjust column widths" is enabled, then the column's
					label will be included in the width calculation if this option
					is enabled. If this option is disabled, and the column contains
					very short values, the column width could be smaller than the
					column's label.
				</para>
				<para>
					This option is also used when manually optimizing the column width,
				</para>
			</simplesect>

			<simplesect id="options-max-column-width">
				<title>Max. column width</title>
				<para>When the initial display size of a column is calculated, or if
					you optimize the column widths to fit the actual data, columns will not exceed this
					width. This is useful when displaying large character columns.
				</para>
			</simplesect>

			<simplesect id="options-min-column-width">
				<title>Min. column width</title>
				<para>When the initial display size of a column is calculated, or if
					you optimize the column widths to fit the actual data, columns will not exceed this
					width.
				</para>
			</simplesect>
			
		</section>
		
		<section id="row-height-settings">
			<title>Row height settings</title>
			<simplesect id="options-auto-optimize-row-height">
				<title>Automatically adjust row height</title>
				<para>
					If this option is enabled, the height of each column is automatically adjusted after data
					retrieval to display as many lines of the column values (for character columns) as possible.
					Note that you can manually optimize the row height using
					<menuchoice><guimenu>View</guimenu><guimenuitem>Optimize row height</guimenuitem></menuchoice>.
				</para>
				<para>
					Not every (character) column is displayed in a manner that multiple lines will be displayed.
					The default setting is to display only <literal>CLOB</literal> columns and <literal>VARCHAR</literal>
					columns with a maximum size of more than 500 characters in multiline mode. 
					The threshold for <literal>VARCHAR</literal> columns can be <link linkend="settings-varchar-multiline-threshold">changed</link>.
				</para>
			</simplesect>

			<simplesect id="options-allow-row-resize">
				<title>Allow row height resizing</title>
				<para>
					If this option is enabled, you can manually adjust the height of each row using the mouse.
					This option does not need to be enabled in order to (automatically) optimize the row height.
				</para>
			</simplesect>

			<simplesect id="options-rowheight-max-lines">
				<title>Max. number of lines</title>
				<para>
					When calculating the optimimal height for each row, the number of lines defined
					with this option will never be exceeded. 
				</para>
			</simplesect>
			
		</section>

		<section id="options-alternating-row-colors">
			<title>Alternate row colors</title>
			<para>
				If this option is selected, the rows in the data table will be displayed
				with alternating background color. You can choose the alternate color (the
				other color is defined by the used <link linkend="options-look-and-feel">Look &amp; Feel</link>)
				with the font chooser next to the checkbox.
			</para>
		</section>

		<section id="options-null-value-color">
			<title>Color for <literal>NULL</literal> values</title>
			<para>
				If a color is defined, NULL values will be highlighted with the selected colors in the result set.
			</para>
		</section>

	</section>

	<section id="options-data-edit">
		<title>Options for data editing</title>

		<section id="options-confirm-update">
			<title>Confirm result set updates</title>
			<para>When this option is enabled, the statements which are sent to the
				database when saving changes to result set table, are displayed before
				execution. The update can be cancelled at that point if the statements
				are not correct. The generated statements can also be saved to a file
				from that window.
			</para>
			<note>
				<para>The statement(s) that are displayed in the confirmation window can not be changed!</para>
			</note>
		</section>

		<section id="warn-pending-changes">
			<title>Confirm discarding changed results</title>
			<para>
				When running a statement that would replace a result that has changes
				that are not saved to the database, you will be prompted whether 
				you want to cancel the current operation that would discard those changes.
			</para>
			<para>
				This applies to statements run in the editor, as well as to changes
				done in the <link linkend="table-data">Data tab of the DbExplorer</link>.
			</para>
			<para>
				You will not be prompted when running statements in the editor, when the
				option <link linkend="display-result">Append results</link> is enabled.
			</para>
		</section>

		<section id="options-highlight-required">
			<title>Highlight required fields</title>
			<para>When editing data either in the result set or in the
				data tab of the DbExplorer, fields that are set to <literal>NOT NULL</literal>
				in the underlying table, will be displayed with a different background color
				if this option is selected.
			</para>
		</section>

		<section id="options-required-field-color">
			<title>Color for required fields</title>
			<para>
				If required fields are highlighted during editing, this option
				defines the background color that is used.
			</para>
		</section>

		<section id="options-pkmapping">
			<title>Default PK Map</title>
			<para>
				This property defines a mapping file for primary key columns. The information
				from that file is read whenever the primary keys for a table of
				cannot be obtained from the database. For a detailed
				description on how to define extra primary key columns, please
				refer to the <link linkend="command-define-pk"><literal>WbDefinePk</literal></link> command.
			</para>
		</section>

	</section>

	<section id="options-dbexplorer">
		<title>DbExplorer options</title>

		<section id="options-explorer-as-tab">
			<title>DB Explorer as Tab</title>
			<para>The Database Explorer can either be displayed as a separate
				window or inside the main window as a another tab. If this option is
				selected, the Db Explorer will be displayed inside the main window. If
				the option Retrieve DB Explorer is checked as well, the current database
				scheme will be retrieved upon starting &wb-productname;
			</para>
		</section>

		<section id="dbexp-options-show-trigger">
			<title>Show trigger panel</title>
			<para>
				By default triggers are shown only in the details of a table. If the
				option "Show trigger panel" is selected, an additional panel will
				be displayed in the DbExplorer that displays all triggers in the
				database independently of their table.
			</para>
		</section>

		<section id="options-retrieve-explorer" xreflabel="Retrieve DB Explorer">
			<title>Automatically retrieve objects</title>
			<para>If this option is enabled, the contents of the database schema is
				retrieved when the DB Explorer is displayed. If this option is not
				checked, either the <guibutton>Refresh</guibutton> button or selecting a schema or
				table type will load the list.
			</para>
		</section>

		<section id="dbexp-options-remember-type">
			<title>Remember object type</title>
			<para>
				The list of objects can be filtered with the dropdown. If the
				option "Remember object type" is selected, the current object type
				will be stored in the workspace of the current connection, and will
				be restored the next time.
			</para>
		</section>

		<section id="dbexp-options-remember-sort">
			<title>Remember sort column</title>
			<para>
				When this option is selected, the sort column in the data display
				of the DbExplorer will be restored after reloading the table data.
			</para>
		</section>

		<section id="dbexp-options-focus-data">
			<title>Focus to data panel</title>
			<para>
				When this option is selected, the focus inside the DbExplorer will
				be set to the data panel, after an object in the list has been selected
				(and the data panel is visible).
			</para>
		</section>

		<section id="dbexp-options-show-focus">
			<title>Show focus</title>
			<para>
				When this option is selected, a rectangle indicating the currently
				focused panel will be displayed, to indicate the component that
				will received keystrokes e.g. shortcuts such as <literal>Ctrl-R</literal>.
			</para>
		</section>

		<section id="dbexp-options-gen-pk">
			<title>Generate PK constraint name</title>
			<para>
				When displaying the SQL source for a table, a name will be generated
				for primary key constraint if the current constraint has no name or
				a system generated name.
				<note>
					If this option is selected, the generated SQL does not reflect
					the real statement that was used to create the table!
				</note>
				System generated names are identified using a regular expression
				that can be <link linkend="manual-setting-system-names">configured</link>.
			</para>
		</section>

	</section>

	<section id="options-window-title">
		<title>Window Title</title>

		<para>
			The title bar of the main window displays displays information
			about the current connection, workspace and editor file. Some
			of these elements can be enabled or disabled with the options
			on this page.
		</para>

		<section id="options-window-title-appatend">
			<title>Application name at end</title>
			<para>
        If this option is enabled, the Application name will be put at the end
        of the window title.
			</para>
		</section>

		<section id="options-window-showworkspace">
			<title>Show Workspace name</title>
			<para>
				If this option is enabled, the currently loaded workspace name
				will be displayed in the main window's title.
			</para>
		</section>

		<section id="options-window-showgroupname">
			<title>Show Profile Group</title>
			<para>
				If this option is enabled, the group of the current connection
				profile will be displayed in the main window's title. The name
				of the current connection profile will always be shown.
			</para>
		</section>

		<section id="options-enclose-group">
			<title>Enclose Group With</title>
			<para>
        If you select to display the current profile's group, you can select a pair
        of characters to put around the group name.
			</para>
		</section>

		<section id="options-group-separator">
			<title>Separator</title>
			<para>
        If you select to display the current profile's name and group, you can select the
        character that separates the two names.
			</para>
		</section>

		<section id="options-editor-filename">
			<title>Editor Filename</title>
			<para>
				If the current editor tab contains an external file, you
				can choose if and which information about the file should be displayed
				in the window title. You can display nothing, only the filename or
				the full path information about the current file. The information
				will be displayed behind the current profile and workspace name.
			</para>
		</section>

	</section>

	<section id="options-sql-formatting">
		<title>SQL Formatting</title>
      <para>
        These options influence the behaviour of the SQL Formatter when
        <link linkend="editor-reformat-sql">reformatting</link> a SQL statement in the editor.
      </para>
			<section id="opt-sql-formatting-maxlength">
				<title>Max. length for sub-select</title>
				<para>
					When the SQL formatter hits a sub-SELECT while
					parsing it will not reformat any statement which is shorter
					then the length specified with this option, i.e. any sub-SELECT
					shorter then this value will be formatted as one single statement without
					line breaks or indention. See <xref linkend="editor-reformat-sql"/> for
					details on how the SQL formatting works.
				</para>
			</section>

			<section id="opt-sql-formatting-selectcols">
				<title>Columns in SELECT</title>
				<para>
					This property defines the number of columns the formatter puts in on line
					when formatting a <literal>SELECT</literal> statement. The default of 1 (one)
					will put each column into a separate line:
				</para>
<programlisting>SELECT p.name,
       p.firstname,
       a.city,
			 a.zip
FROM person p JOIN address a ON (p.person_id = a.person_id);</programlisting>
				<para>
					If this is set to 2, this
					would result in the following formatted SELECT:
				</para>
				<programlisting>
SELECT p.name, p.firstname,
       a.city, a.zip
FROM person p JOIN address a ON (p.person_id = a.person_id);</programlisting>
				<para>
					The above example would list all columns in a single line, if this option
					is set to 4 (or a higher value):
				</para>
				<programlisting>
SELECT p.name, p.firstname, a.city, a.zip
FROM person p JOIN address a ON (p.person_id = a.person_id);</programlisting>
			</section>

			<section id="opt-sql-formatting-insertcols">
				<title>Columns in INSERT</title>
				<para>
					This property defines the number of columns the formatter puts in on line
					when formatting an <literal>INSERT</literal> statement. A value of one will
					list each column in a separate line in the <literal>INSERT</literal> part and the <literal>VALUES</literal> part
				</para>
<programlisting>
INSERT INTO PERSON
(
  id,
  firstname,
  lastname
)
VALUES
(
  42,
  'Arthur',
  'Dent'
);</programlisting>
         <para>
					 When setting this value to 2, the above example would be formatted as follows:
				 </para>

<programlisting>INSERT INTO PERSON
  (id, firstname,
   lastname)
VALUES
  (42, 'Arthur',
   'Dent');</programlisting>

			</section>

			<section id="opt-sql-formatting-updatecols">
				<title>Columns in UPDATE</title>
				<para>
					This property defines the number of columns the formatter puts in on line
					when formatting an <literal>UPDATE</literal> statement. A value of 1 (one) will
					put each column into a separate line:
				</para>
<programlisting>
UPDATE person
   SET firstname = 'Arthur',
       lastname = 'Dent'
WHERE id = 42;</programlisting>
				<para>
					With a value of 2, the above example would be formatted as follows:
				</para>
<programlisting>
UPDATE person
   SET firstname = 'Arthur', lastname = 'Dent'
WHERE id = 42;</programlisting>

			</section>

			<section id="opt-sql-formatting-quoted-elements">
				<title>Quoted elements per line</title>
				<para>
					This optioin is used when changing the selected text into elements suitable for an <literal>IN</literal> list
					using <link linkend="editor-make-in-list"><menuchoice><guimenu>SQL</guimenu><guimenuitem>Create SQL List</guimenuitem></menuchoice></link>.
					The number of values that are kept on a single line is controlled with this option.
				</para>
			</section>

			<section id="opt-sql-formatting-other-elements">
				<title>Other elements per line</title>
				<para>
					This option defines how many values will be put into a single
					line when creating non-quoted elements (<link linkend="editor-make-in-list">Create non-char SQL List</link>).
				</para>
			</section>

			<section id="opt-formatter-lowercase-functions">
				<title>Lowercase functions</title>
				<para>
					If this option is selected, standard ANSI functions will converted to lowercase
					when formatting a SQL statement.
				</para>
			</section>

	</section>

	<section id="options-sql-generation">
		<title>SQL Generation</title>

		<section id="options-generated-update">
			<title>Generated UPDATE statements</title>
			<para>
				If formatting of <literal>UPDATE</literal> statements is enabled,
				the threshold defines how many columns have to be present for a single
				<literal>UPDATE</literal> statement in order to put each column
				into a separate line. If the number of columns is lower then this value
				they will remain on one line. The keywords (UPDATE, WHERE) will
				still be formatted into new lines.
			</para>
		</section>

		<section id="options-generated-insert">
			<title>Generated INSERT statements</title>
			<para>
				If formatting of <literal>INSERT</literal> is enabled, the way they
				are formatted can be controlled with several values.
			</para>
			<section id="opt-sql-generate-column-threshold">
				<title>Column threshold</title>
				<para>
					If the number of columns in the statement exceeds this value, the columns
					will be spread over several lines. The number of columns that are put
					into each line is controlled using the option "Columns per line".
				</para>
			</section>

			<section id="opt-sql-generate-columns-perline">
				<title>Columns per line</title>
				<para>
					If the number of columns in the option "Column threshold" is exceeded,
					this option controls how many columns are put into each line
				</para>
			</section>
		</section>

		<section id="opt-sql-export-include-owner">
			<title>Include owner in export</title>
			<para>
				This setting controls whether &wb-productname; uses the onwer (schema)
				when creating SQL scripts during exporting data (through WbExport
				or "Save as"). When this option is selected, the usage of the schema
				depends on the <link linkend="settings-db-ignoreschema">ignore schema</link> setting
				that controls ignoring certain schemas for specific DBMS.
				When this is option is not selected, the schema/owner will never be used for SQL scripts.
			</para>
		</section>

		<section id="options-default-copy-literaltype">
			<title>Date literals for clipboard</title>
			<para>
				Defines the date literal format to be used when copying data
				as SQL statements <emphasis>to the clipboard</emphasis>. For a detailed description
				of the different formats please refer to the
				<link linkend="export-sql-literal-formats">WbExport</link> description.
				This option does not influence the default format used by the <literal>WbExport</literal>
				command.
			</para>
			<note><para>
				When you copy data as "Text" (tab-separated) to the clipboard, the date and
				timestamp format from the <link linkend="options-date-format">general options</link> is used.
			</para></note>
		</section>

		<section id="options-default-export-literaltype">
			<title>Date literals for WbExport</title>
			<para>
				Defines the date literal format to be used for the <link linkend="command-export">WbExport</link>
				command. The value of this option is used if the <literal>-sqlDateLiterals</literal>
				switch is not supplied when running <literal>WbExport</literal>.
				This default value is reported when <literal>WbExport</literal> is executed
				without parameters.
			</para>
		</section>

		<section id="options-default-diff-literaltype">
			<title>Date literals for WbDataDiff</title>
			<para>
				Defines the date literal format to be used for the <link linkend="command-data-diff">WbDataDiff</link>
				command. The value of this option is used if the <literal>-sqlDateLiterals</literal>
				switch is not supplied when running <literal>WbDataDiff</literal>.
				This default value is reported when <literal>WbDataDiff</literal> is executed
				without parameters.
			</para>
		</section>

	</section>

	<section id="options-external-tools">
		<title>External tools</title>
		<para>
			On this page, you can define external tools (programs). Currently the only
			place where this is used, is in the <link linkend="update-blob-editmode">blob info dialog</link>,
			to open the BLOB data with one of the defined external tools. This could
			be a program to display images (like <ulink url="http://www.xnview.org">XnView</ulink>),
			<ulink url="http://www.adobe.com/products/acrobat/index.html">Acrobat Reader</ulink>
			to display PDF documents, <ulink url="http://www.openoffice.org">OpenOffice</ulink> to display
			office documents or a text editor (like <ulink url="http://www.pspad.com">PSPad</ulink>) to
			display text files.
		</para>
	</section>

	<section id="options-look-and-feel">
		<title>Look and Feel</title>
		<para>
			If you want to use additional Look and Feels that are not part
			of the JDK, you can specify them here.
		</para>
		<para>
			A Look And Feel definition consists of a name, the class name to be used and
			the location of the JAR file that provides the look and feel implementation.
			The class name that has to be used should be available in the documentation
			of the look and feel of your choice. The name is &wb-productname; internal
			and is only used when displaying the list of available Look and Feels.
		</para>
		<para>
			When you switch the current Look &amp; Feel, you will need to restart the
			application to activate the new look and feel. Note that if you switch
			the current Look &amp; Feel it will be changed, regardless whether
			you close the options dialog using Cancel or OK.
		</para>
		<note><para>
			The current look and feel is only changed when you click on the
			<guibutton>Make current</guibutton> button. Simply selecting a different entry
			in the list on the left side will <emphasis role="bold">not</emphasis>
			change the look and feel.
		</para></note>
	</section>

</section>
